const int N = 1000;
const int M = 1000;

bool cmp();

class Solution {
public:
    int maxProfitAssignment(vector<int>& difficulty, vector<int>& profit, vector<int>& worker) {
        // dp[i][j] - max profit by assigning the first i workers to the first j jobs
        // dp[i][j] = max()

    }
};